<%= link_to "Category Index", categories_path %>
<div class="jumbotron">
  <h2><%= @category.name %></h2>
    <% if @user == nil %>
      <p> You can only view your category ideas if you are logged in. </p>
    <% else %>
      <ul><% @users_ideas.each do |idea|  %>
        <li><%= link_to idea.name, category_idea_path(@category, idea) %></li>
          <% end %>
    <% end %></ul>


  <%= link_to "Contribute an Idea", new_idea_path %>
</div>
